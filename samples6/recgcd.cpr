var num;

fun int gcd(m, n) {
  if (n == 0)
    return m;
  else
    return gcd(n, m%n);
}

fun void main() {
  var m, n;
  num = 3;
  while (num > 0) {
    m = input();
    n = input();
    output(gcd(m, n));
    num = num-1;
  }
}
