var it;

fun void readit() {
  it = input();
}

fun void writeit() {
  output(it);
}

fun void multiplyit(factor) {
  it = factor*it;
}

fun void mult(factor) {
  readit();
  multiplyit(factor);
  writeit();
}

fun void main() {
  mult(100);
  mult(-50);
}
